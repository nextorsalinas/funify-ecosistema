{
  "name": "funify-ecosistema",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "NEXT_PUBLIC_API_URL=https://pruebas.funifay.com/backend/public npm --prefix frontend install && NEXT_PUBLIC_API_URL=https://pruebas.funifay.com/backend/public npm --prefix frontend run build",
    "start": "npm --prefix frontend run start",
    "dev": "npm --prefix frontend run dev",
    "postinstall": "npm --prefix frontend install"
  }
}
